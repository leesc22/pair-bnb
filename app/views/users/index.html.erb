<% @users.each do |user| %>
	<p><%= user.id %>. <%= user.name %> | <%= link_to 'Edit User', edit_user_path(user) %> | <%= link_to 'Delete User', user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %></p>
<% end %>