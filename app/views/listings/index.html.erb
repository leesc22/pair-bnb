<h1>This is listings/index page</h1>

<% @listings.each do |listing| %>
	<%= link_to listing_path(listing), target: "_blank" do %>
		<div>
			<%= listing.title %>
			<%= listing.address %>
			<%= listing.price_per_night %>
		</div>
	<% end %>
<% end %>